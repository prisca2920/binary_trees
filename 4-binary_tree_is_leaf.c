#include "binary_trees.h"

/**
 * binary_tree_is_leaf - checks if node is a leaf
 * @node: pointer of the node to be checked
 * Return: 1 if node, 0 fail
 */

int binary_tree_is_leaf(const binary_tree_t *node)
